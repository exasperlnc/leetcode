def minimum_effort_path(heights)
  # iterate through heights

  # save all possible paths, and keep the max_dif saved with each path

  # sort the paths by smallest max_dif

  # return paths[0].max_dif
  
end